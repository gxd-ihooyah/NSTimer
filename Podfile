# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target '计时器' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for 计时器

  target '计时器Tests' do
    inherit! :search_paths
    # Pods for testing
     pod 'SDWebImage'
  end

  target '计时器UITests' do
    # Pods for testing
  end

end
